# Audio Directory

Add music tracks for the playlist section here.

For best results:
- Place your `.mp3` files in this folder. 
- You can name them `song1.mp3` and `song2.mp3` to instantly work with the current code.
- Otherwise, update the `src` attribute of the `<source>` tags in `templates/index.html` to reflect the exact name of your files.
