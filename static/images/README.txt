# Images Directory

Add real images of Mansi here to replace the placeholders.

For best results:
- Place your `.jpg` or `.png` images in this folder.
- Update the `src` attributes of the `<img>` tags inside `templates/index.html` to point to `{{ url_for('static', filename='images/your_image.jpg') }}`.
